version: '2'
services:
  haproxy:
    metadata:
      password: ${password}
      dbname: ${dbname}
      user: ${user}
      version: ${version}
    %{~ if ! global ~}
    scale: 2
    %{~ endif ~}
    start_on_create: true
